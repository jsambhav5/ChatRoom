import React from "react";

const StepPassword = ({ onNext }) => {
	return (
		<>
			<div>
				<div>StepPassword</div>
			</div>
			<button onClick={onNext}>Next</button>
		</>
	);
};

export default StepPassword;
